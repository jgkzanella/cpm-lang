use "stdio";

def fatorial(int n) :: int {
    int f;
    while (x > 1) {
        f = f * n;
        n--;
    }
    return f;
};

def main() :: int {
    int x = 1;
    int n = 0;
    read(n);
    n = fatorial(n);
    print(n);
    return 0;
};